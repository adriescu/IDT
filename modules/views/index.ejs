<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bienvenido</title>
    <link rel="stylesheet" href="/css/general.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
    <style>
        h2{
            color: white;
            font-size: 4.2rem;
        }
        section{
            width: 100vw;
            text-align: center;
        }
        #id-input, #nombre-input{
            border-radius: 17px;
            width: 40vw;
            min-width: 300px;
            height: 3rem;
            font-size: 2rem;
            border: 2px solid black;
            margin-bottom: 2rem;
            padding: 0 1rem;
            transition: all 0.5s;
        }
        #id-input:hover, #nombre-input:hover, #id-input:focus, #nombre-input:focus{
            border-radius: 20px;
            background-color: rgb(175, 170, 170);
            color: rgb(38, 36, 36);
        }

        form{
            display: flex;
            align-items: center;
            justify-content: center;
        }

        form button{
            border-radius: 17px;
            height: 3rem;
            border: 2px black solid;
            padding: 0;
            margin-left: 0.4rem;
            margin-bottom: 2rem;
            transition: all 0.5s;
            width: 3rem;
        }
        
        form button:hover{
            cursor: pointer;
            border-radius: 20px;
        }
        .busqueda{  
            width: 57vw;
            padding: 1rem;
            margin:0 auto;
            border-radius: 40px;
        }
        p{
            color: white;
            font-family: Arial, Helvetica, sans-serif;
            font-size: 1.3rem;
        }
        body{
            background-color: rgb(47, 45, 45);        }
    </style>
</head>
<body>
    <main>
        <section>
            <h2>Bienvenido</h2>
            <div class="busqueda">
                <p>Buscar por ID</p>
                <form action="/item">
                    <input type="text" name="id" id="id-input">
                    <button type="submit">
                        <span class="material-symbols-outlined">search</span>
                    </button>
                </form>
                <p>Buscar por nombre</p>
                <form action="/lista">
                    <input type="text" name="nombre" id="nombre-input">
                    <button type="submit">
                        <span class="material-symbols-outlined">search</span>
                    </button>
                </form>
            </div>
        </section>
    </main>
    <script>
        console.log( getComputedStyle(document.querySelector("section")));
    </script>
</body>
</html>